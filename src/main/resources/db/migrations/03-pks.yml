databaseChangeLog:
  - changeSet:
      id: 3
      author: Andrew
      changes:
        - sql:
            sql: |
              ALTER TABLE PEDIDO  ADD (IDPEDIDO INTEGER GENERATED BY DEFAULT AS IDENTITY);
              ALTER TABLE ITEMPEDIDO  ADD (IDITEMPEDIDO INTEGER GENERATED BY DEFAULT AS IDENTITY);
              ALTER TABLE DAVSOVIS  ADD (IDDAVSOVIS INTEGER GENERATED BY DEFAULT AS IDENTITY);
              ALTER TABLE CLIENTE  ADD (IDCLIENTE INTEGER GENERATED BY DEFAULT AS IDENTITY);